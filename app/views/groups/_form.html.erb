<%= form_for @group do |f| -%>

<div class="form_group">
    <div class="form-group string optional">
        <%= f.label "title", :class => "string optional control-label" %>
        <%= f.text_field :title, :class => "string option form-control form-control" %>
    </div>

    <div class="form-group string optinal" >
        <%= f.label :"desctiption", :class =>"string oprional control-label" %>
        <%= f.text_area :description, :class =>"strin optional form-control"%>
    </div>

</div>

<%= f.submit "Submit", :disable_with => 'Submiting...'%>
<% end %>
